__pycache__/
*.pyc
.venv/
venv/
.streamlit/secrets.toml
.env
.DS_Store

# Model weights are large (140MB combined) - track with Git LFS instead
# of committing raw binaries to the regular git history.
# git lfs track "*.pth"
